const name: { name: string } = { name: "Alice" }; 
const age: { age: number } = { age: 30 }; 
const isOnline: { isOnline: boolean } = { isOnline: true }; 

console.log(`Name: ${name.name}`); 
console.log(`Age: ${age.age}`); 
console.log(`Online: ${isOnline.isOnline}`);